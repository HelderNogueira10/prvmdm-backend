package com.privguard.mdm.server.command_types;

import org.springframework.data.jpa.repository.JpaRepository;

public interface CommandTypeRepository extends JpaRepository<CommandTypeEntity, Long> {
}
